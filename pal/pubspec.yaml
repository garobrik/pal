name: pal
description: minimal new thing

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.5
  infra_widgets:
    path: ../infra_widgets

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
